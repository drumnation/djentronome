import { TimingFeedbackData } from '../../NoteHighwayCanvas.types';

/**
 * Props for the TimingFeedback component
 */
export type TimingFeedbackProps = TimingFeedbackData;

/**
 * Represents the accuracy level styling data
 */
export interface AccuracyDisplayData {
  color: string;
  text: string;
} 