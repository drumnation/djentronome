export enum AppRoutes {
  HOME = '/',
  GAME = '/game',
  SETTINGS = '/settings',
  SONG_SELECTION = '/songs',
  RESULTS = '/results',
}

export const routes = {
  home: AppRoutes.HOME,
  game: AppRoutes.GAME,
  settings: AppRoutes.SETTINGS,
  songSelection: AppRoutes.SONG_SELECTION,
  results: AppRoutes.RESULTS,
}; 